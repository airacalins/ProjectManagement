export interface IDashboard {
    slots: number,
    availableSlots: number,
    rentedSlots: number,
    tenants: number,
    unpaidInvoices: number,
    latePayments: number,
    pendingPayments: number
}