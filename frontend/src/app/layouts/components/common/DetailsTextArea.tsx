import { Table, TextArea } from "semantic-ui-react";

interface Props {
    label: string,
    input: any
}

const DetailsTextArea = ({ label, input }: Props) => {
    return (
        <h1>hi</h1>
    );
}

export default DetailsTextArea;